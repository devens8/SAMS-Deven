@model IEnumerable<SAMS.Models.SynnLabQRNodeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ScannerMacAddressMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModelNumberMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScannerDeviceIPAddressMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScannerLabelMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ScannerMacAddressMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModelNumberMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScannerDeviceIPAddressMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScannerLabelMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ScannerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ScannerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ScannerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
