@model IEnumerable<SAMS.Models.RoomLocationInfoModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoomNumberMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WingNameMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomCodeMod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomScannerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumberMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WingNameMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomCodeMod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomScannerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
